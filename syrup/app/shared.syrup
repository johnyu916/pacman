
(object value) get_view_old(object state, string name):
    for view in state.game.views:
        if view.name == name:
            value = view
            return
    value = null

(object view) view_new(string name):
    view = {
        "name": name,
    }
